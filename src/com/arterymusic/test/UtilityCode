String idStr = request.getParameter("id");
int id;
if (idStr == null || idStr.isEmpty() || !idStr.matches("\\d+")) {
    // 参数为空或为非数字
    // 进行相应的异常处理
    request.setAttribute("updateMsg", "参数错误");
    request.getRequestDispatcher("test.jsp").forward(request, response);
    return;
} else {
    // 参数为数字
    id = Integer.parseInt(idStr);
}
##############################################

//        if (account != null && account.length() > 0) {
//            // 封装对象
//            User addUser = new User(account, password,email);
//            // ...
//        } else {
//            // account 为空的处理逻辑
//        }

##########################验证用户名是否符合格式要求
    public boolean isValidUsername(String username){
        if (username == null || username.length()<6|| username.length()>11){
            return false;
        }
        for (int i = 0; i<username.length(); i++){
            char ch = username.charAt(i);
            // isLetterOrDigit = 字母或者数字
            if (!Character.isLetterOrDigit(ch) && ch != '_' && ch != '-') {
                return false;
            }
        }

        return true;
    }
##############################::before 伪元素的使用
<div class="header-menu">
   <span class="backstage iconfont">音乐后台管理</span>
</div>
-------->相关的css
<style>
      span {
        position: relative;
        padding-left: 20px;
      }
      backstage::before {
        content: "\f0a4";
        position: absolute;
        left: 0;
        top: 0;
      }
</style>